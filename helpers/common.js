// eslint-disable-next-line
async function directRead(mongoConnection, elasticsearchConnection, config) {
  // to be used
}

// eslint-disable-next-line
function changeStreamlisten(mongoConnection, elasticsearchConnection, config) {
  // to be used
}

module.exports = {
  directRead,
  changeStreamlisten,
};
